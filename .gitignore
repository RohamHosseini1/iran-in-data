# OS/editor cruft
.DS_Store
__pycache__/
*.pyc

# Backup files created during in-place CSV patches (kept on disk for audit trail,
# not worth versioning individually since git history already tracks the diff)
*.bak-*

# Generated build artifacts -- fully regeneratable from data/charts/ via
# scripts/build_downloads.py, no need to version the zips themselves
downloads/
